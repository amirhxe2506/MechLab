# Core / shared models will be introduced in a later phase.
# Phase 1 only establishes the app skeleton so it can be registered
# in INSTALLED_APPS and picked up cleanly by Django's app registry.
from django.db import models  # noqa: F401
