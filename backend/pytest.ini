[pytest]
DJANGO_SETTINGS_MODULE = config.settings

# The calculation-engine tests (tests/test_*.py) are plain function-based
# pytest tests with no Django dependency. Django app tests follow the
# `tests.py` convention generated by `startapp`. Both patterns must be
# collected.
python_files = test_*.py tests.py

addopts = -ra
